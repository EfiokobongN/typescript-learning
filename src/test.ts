let project = 5;
let drive: string = 'Slow';

let nothing = null;
let notExist = undefined;


let now = new Date();

let colors = ['green', 'yellow', 'black', 'red', 99, 20];

let threeDimension: string[][][] = [[['one', 'two', 'three']]]; //array of three level
